#!/bin/bash
echo "🔍 Running Flutter asset validator..."

# Run the Dart validator script
dart run tool/validate_assets.dart
STATUS=$?

if [ $STATUS -ne 0 ]; then
  echo "❌ Asset validation failed. Commit aborted."
  exit 1
fi

echo "✅ Asset validation passed. Continuing with commit."
exit 0
// This script validates that all font asset files referenced in pubspec.yaml exist.
import 'dart:io';